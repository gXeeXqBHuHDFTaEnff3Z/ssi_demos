<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.vertedge.ssicapabilitymanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_certificate"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_certificate"
        android:supportsRtl="true"
        android:theme="@style/Theme.SSICapabilityManager">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SSICapabilityManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>